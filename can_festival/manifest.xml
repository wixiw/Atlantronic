<package>
    <description brief="CanFestival, CanOpen framewwork.">
	CanFestival is a framework interfacing on top of can drivers (such as socketCan) to handle the CanOpen standards.
    </description>
    <author>http://www.canfestival.org/</author>
    <license>LGPL</license>
    
    <depend package="rtt" />
    <depend package="ocl" />
    <depend package="rosbuild" />
	
	<export>
		<cpp os="gnulinux" cflags="-I${prefix}/include -I${prefix}/include/canfestival" lflags="-L${prefix}/lib -llibcanfestival_unix.a -llibcanfestival.a"/>
		<rosbuild cmake_directory="${prefix}/cmake"/>
	</export>
	<!--
	<export>
		<cpp os="xenomai" cflags="-I${prefix}/include -I${prefix}/include/canfestival -I/usr/include/xenomai" lflags="-L${prefix}/lib -llibcanfestival_unix.a -llibcanfestival.a -L/usr/lib -Wl,-rpath /usr/lib -lnative -lxenomai -lpthread -lrtdm -ldl"/>
		<rosbuild cmake_directory="${prefix}/cmake"/>
	</export>-->
    
</package>

