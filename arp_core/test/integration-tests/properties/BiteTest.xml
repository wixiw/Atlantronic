<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "cpf.dtd">
<properties>
  <simple name="propAutoLoadScript" type="string"><description>If non empty, the component will automatically load this script during configure() (le dossier ops est automatiquement ajouté, l'extension est mise par le soft)</description><value>BiteTest</value></simple>
  <simple name="propAutoLoadStateMachines" type="string"><description>If non empty, the component will automatically load this state machine during configure() (le dossier fsm-osd est automatiquement ajouté, l'extension est mise par le soft)</description><value></value></simple>
</properties>
